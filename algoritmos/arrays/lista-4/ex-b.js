/*b) Crie um array com os números 1, 2 e 3. Remova o último número e exiba o número
removido no console (usando pop()).*/
// Dica – Lembre-se que o pop() retorna o item que foi removido. Isso significa que é possível
// armazená-lo em uma variável. Exemplo: let removido = vetor.pop()
// shift() – Remove o primeiro item de um array
const array = [1, 2, 3]
console.log(array.pop())